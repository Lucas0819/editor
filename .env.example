# Optional environment variables — the editor works without any of these

# Google Maps API key (enables address search)
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Dev server port (default: 3000)
# PORT=3000

# Optional: absolute base URL for `/items/...` assets (defaults to same origin / `public/`)
# NEXT_PUBLIC_ASSETS_CDN_URL=https://editor.pascal.app
